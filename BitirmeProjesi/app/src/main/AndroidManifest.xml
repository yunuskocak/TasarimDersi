<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yunusmac.bitirmeprojesi" >

    <uses-sdk android:minSdkVersion="14" />

    <android:uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <android:uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".KarsilamaSayfasi"
            android:label="@string/title_activity_karsilama_sayfasi" >
        </activity>
        <activity
            android:name=".IcerikEkrani"
            android:label="@string/title_activity_icerik_ekrani" >
        </activity>
        <activity
            android:name=".icerikislemleri"
            android:label="@string/title_activity_icerikislemleri" >
        </activity>
        <activity
            android:name=".sifre_hatirlat"
            android:label="@string/title_activity_sifre_hatirlat" >
        </activity>
        <activity
            android:name=".dialog_popup"
            android:label="@string/title_activity_dialog_popup" >
        </activity>
    </application>

</manifest>
